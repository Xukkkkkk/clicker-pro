# 协作约定

- 用户要求：每次修改完成并通过相关验证后，自动创建 Git 提交，并推送到当前分支配置的远程仓库，无需再次确认。
- 如果未配置远程地址，先完成本地提交，再向用户索取仓库地址；不要猜测地址或宣称推送成功。
- 推送失败时说明实际原因；不要强制推送或覆盖远程历史。
- 统一发布到 `dist/ClickerPro.exe`，不按主题或功能保留多个 EXE。`build/` 和 `dist/` 按现有规则忽略，不加入代码提交。
